// Loops
// repeatedly run a block of code while condition is true
// while Loops
// TURN OFF AUTOSAVE

let amount = 10;

while(amount > 0) {
    console.log('I have ' + amount + " dollars and I'm going to the mall");
    amount--; // without the break the loop will run endlessly browser will crash
}